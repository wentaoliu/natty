<div class="ui vertical segment">

  <div class="ui huge header">
    <div class="ui label">
      <span class="fa fa-bullhorn fa-fw"></span>
      <%=t 'sidebar.bulletins' %>
    </div>
    <%= @bulletin.title %>
  </div>

  <p>
    <%= link_to t('.edit'), edit_bulletin_path(@bulletin), class:"ui button" %>
  </p>
</div>
<div class="ui piled segment"><%= raw @bulletin.content %></div>
