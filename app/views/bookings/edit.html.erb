<div class="ui secondary menu">
  <div class="item">
    <div class="ui huge breadcrumb">
      <%= link_to t('module.instruments'), instruments_path, class:'section' %>
      <i class="right angle icon divider"></i>
      <%= link_to @instrument.name, @instrument, class: "section" %>
      <i class="right angle icon divider"></i>
      <%= link_to l(@booking.starts_at, format: :long), instrument_bookings_path(@instrument), class: "section" %>
      <i class="right angle icon divider"></i>
      <div class="active section"><%=t '.edit' %></div>
    </div>
  </div>
  <div class="right menu">
    <%= link_to [@instrument, @booking], class:"item" do %>
      <i class="remove icon"></i><%=t '.cancel' %>
    <% end %>
  </div>
</div>

<%= render 'form' %>
