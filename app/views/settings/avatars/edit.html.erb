<%= form_for(@user, url: settings_avatar_path, html:{class:'ui form segment'}) do |f| %>

  <div class="field">
    <%= f.label :avatar, t('.avatar') %>
    <%= f.file_field :avatar%>
  </div>

  <%= f.submit t('.submit'), class:"ui blue button" %>

<% end %>
