<div class="ui secondary menu">
  <div class="item">
    <div class="ui huge breadcrumb">
      <%= link_to t('module.inventories'), inventory_path(@inventory), class: "section" %>
      <i class="right angle icon divider"></i>
      <%= link_to @inventory.item_name, @inventory, class: "section" %>
      <i class="right angle icon divider"></i>
      <div class="active section"><%= t '.edit' %></div>
    </div>
  </div>
  <div class="right menu">
    <%= link_to inventories_path, class:"item" do %>
      <i class="remove icon"></i><%=t '.cancel' %>
    <% end %>
  </div>
</div>

<%= render 'form' %>
