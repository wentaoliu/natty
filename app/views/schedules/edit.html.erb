<div class="ui secondary menu">
  <div class="item">
    <div class="ui huge breadcrumb">
      <%= link_to t('module.schedule'), schedules_path, class:'section' %>
      <i class="right angle icon divider"></i>
      <div class="active section"><%=t '.edit' %></div>
    </div>
  </div>
  <div class="right menu">
    <%= link_to schedule_path(@schedule), class:"item" do %>
      <i class="remove icon"></i><%=t '.cancel' %>
    <% end %>
  </div>
</div>

<%= render 'form' %>
