<div class="ui vertical segment">

  <div class="ui huge header">
    <div class="ui label">
      <span class="fa fa-pencil fa-fw"></span>
      <%=t 'sidebar.wikis' %>
    </div>
    <%= @wiki.title %>
  </div>

  <div class="ui visible message">
    <%=t '.message',{author: @article.user.name, time:(l @article.created_at,:format=>:long)} %>
  </div>
  <p>
    <%= link_to t('.edit'), edit_wiki_article_path(@wiki,@article), class:"ui button" %>
    <%= link_to t('.modify'), edit_wiki_path(@wiki), class:"ui button" %>
    <%= link_to t('.history'), wiki_articles_path(@wiki), class:'ui button' %>
  </p>
</div>
<div class="ui piled segment"><%= raw @article.content %></div>
